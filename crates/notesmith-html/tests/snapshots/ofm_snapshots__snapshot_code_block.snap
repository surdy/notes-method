---
source: crates/notesmith-html/tests/ofm_snapshots.rs
expression: html
---
<p>Syntax highlight is supported with the language specified after the first set of backticks. We use prismjs for syntax highlighting, a list of supported languages can be found <a href="https://prismjs.com/#supported-languages">at their site</a></p>
<pre><code>```js
function fancyAlert(arg) {
  if(arg) {
    $.facebox({div:'#foo'})
  }
}
```</code></pre>
<pre><code class="language-js">function fancyAlert(arg) {
  if(arg) {
    $.facebox({div:'#foo'})
  }
}
</code></pre>
<pre><code class="language-md">    Text indented with a tab is formatted like this, and will also look like a code block in preview. 
</code></pre>
<pre><code>Text indented with a tab is formatted like this, and will also look like a code block in preview. 
</code></pre>
